{
  "name": "Custom Development Environment",
  "image": "claude-base:latest"
  
  // Minimal custom stack - just uses base image with universal features
  // postCreateCommand is inherited from base template for single source of truth
}