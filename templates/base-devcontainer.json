{
  "name": "Claude Development Container",
  
  // Initialize command writes main repo path to worktree.env file for mount configuration
  "initializeCommand": ".devcontainer/../scripts/get-main-repo-path.sh ${localWorkspaceFolder}",
  
  // Post-create command runs inside container after creation
  // This sets up worktree environment variables and configures the git wrapper
  "postCreateCommand": "sudo startup.sh && .devcontainer/configure-git-wrapper.sh",
  
  // Mount configuration using dynamically resolved main repo path from worktree.env file
  "mounts": [
    "source=${localEnv:HOME}/.claude,target=/home/claude-user/.claude,type=bind",
    "source=$(cat ${localWorkspaceFolder}/.devcontainer/worktree.env 2>/dev/null || echo ${localWorkspaceFolder}),target=/main-repo,type=bind,consistency=cached"
  ],
  
  // Environment variables for git wrapper (set automatically by startup script in worktrees)
  "containerEnv": {},
  
  // Container configuration
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "remoteUser": "claude-user",
  
  // VS Code customizations - common across all stacks
  "customizations": {
    "vscode": {
      "extensions": [
        // Claude Code (universal)
        "anthropic.claude-code",
        
        // Git (universal)
        "eamodio.gitlens",
        "github.vscode-pull-request-github",
        
        // Development tools (universal)
        "ms-vscode.vscode-json",
        "redhat.vscode-yaml",
        "ms-vsliveshare.vsliveshare"
      ],
      "settings": {
        // Git configuration (universal)
        "git.enableSmartCommit": true,
        "git.confirmSync": false,
        "git.autofetch": true,
        
        // Editor configuration (universal)
        "editor.formatOnSave": true,
        
        // Terminal configuration (universal)
        "terminal.integrated.defaultProfile.linux": "zsh",
        
        // File associations (universal)
        "files.associations": {
          "*.env.*": "properties"
        }
      }
    }
  },
  
  // Features and additional configuration
  "features": {
    // Claude MCP servers (universal)
    "ghcr.io/visheshd/claude-devcontainer/claude-mcp:1": {
      "servers": "serena,context7"
    },
    
    // Add GitHub CLI (universal)
    "ghcr.io/devcontainers/features/github-cli:1": {
      "version": "latest"
    }
  },
  
  // Lifecycle hooks
  "onCreateCommand": {
    "install-deps": "echo 'Base container ready - stack-specific setup will run next'"
  },
  
  // Resource limits (can be overridden by stacks)
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb"
  }
}